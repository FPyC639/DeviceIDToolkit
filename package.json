{
  "name": "DeviceIDToolkit",
  "version": "1.0.0",
  "description": "Hardware ID",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . DeviceIDToolkit --platform=win32 --arch=x64 --out=dist --extra-resource=static --overwrite force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "electron": "^32.0.1",
    "electron-packager": "^17.1.2"
  },
  "build": {
    "files": [
      "dist/**/*",
      "static/**/*"
    ]
  }
}
